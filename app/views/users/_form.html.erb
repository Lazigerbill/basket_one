<h2>My favorite 3 stocks:</h2>

<table>
	<th> Ticker symbol </th>
	<th> Company </th>
	<th> Remove </th>
	<% @user.stocks.each do |stock| %>
		<tr>
			<td> <%= stock.yahoo_symbol %> </td>
			<td> <%= stock.company_name %> </td>
			<td> <%= link_to 'Remove', stock, method: :delete, data: { confirm: 'Are you sure?' } %> </td>
		</tr>
	<% end %>
</table>

<%= form_tag edit_user_path(@user), method: :get, authentication: false do %>
<%= label_tag :search, "Symbol lookup:" %>
<%= text_field_tag :search, nil,  :placeholder => "Company name" %>
<%= submit_tag "Look up" %>
<% end %>

<% if @results %>
	<%= form_tag user_stocks_path(@user), :method => "post" do %>
	<table>
		<% @results.each do |result| %>
		<tr>	
			<td>
				<%= radio_button_tag 'index', result["symbol"] %> 
				<%= label_tag 'index', result["symbol"] %> 
				<%= hidden_field_tag :company_name, result["name"] %>
				<%= hidden_field_tag :twitter_symbol, "$" + result["symbol"] %>
			</td>

			<td>
				<%= label_tag 'index',  result["name"] %>
			</td>

			<td>		
				<%= label_tag 'index',  result["exchDisp"] %>
			</td>								
		</tr>
		<% end %>
	</table>
	<%= submit_tag 'Add to favorites' %>

<% end %>
<% end %>


